const User = require("../models/User");
const Message = require("../models/Message");

// exports.messageLists = (req, res) => {
//   const user = req.user;
//   const friendId = req.params.friend;

//   const messages = await Message.findOne({participants: []})
// };
